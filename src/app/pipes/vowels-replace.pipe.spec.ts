import { VowelsReplacePipe } from './vowels-replace.pipe';

describe('VowelsReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new VowelsReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
